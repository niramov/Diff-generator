name: Node CI

on: push

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Get repository code
        uses: actions/checkout@v2
      - name: Installing packet
        run: make install
      - name: Running linter
        run: make lint
      - name: Testing packet
        run: npm run test
        if: ${{ always() }}
